# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
storybook-static
coverage
test-results
*.local

# Environment files
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Internal design notes — keep local, do not publish.
# Takedown completed 2026-05-06: docs/superpowers/ + docs/session-*.md
# scrubbed from public history via git-filter-repo. Closed PR refs
# (/pull/1..3) still expose the old diffs; accepted as out-of-scope.
# These rules prevent any future internal notes from being committed.
docs/superpowers/
docs/session-*.md
docs/codex-*.md

# Internal team rules. AGENTS.md mirrors workspace policy that the
# multi-agent setup reads; not useful publicly and may evolve fast.
AGENTS.md
